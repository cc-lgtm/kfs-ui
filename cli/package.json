{
  "name": "fks-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "preview": false,
  "scripts": {
    "dev": "esbuild --bundle ./index.ts --format=cjs --platform=node --outdir=./lib --watch",
    "build": "esbuild --bundle ./index.ts --format=cjs --platform=node --outdir=./lib",
    "cli": "node ./lib/index.js create"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "commander": "^8.3.0",
    "esbuild": "^0.14.10",
    "fs-extra": "^10.0.0",
    "inquirer": "^8.2.0",
    "kolorist": "^1.5.1"
  }
}
